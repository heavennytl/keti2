# 《端边云协同部署与运行系统》PRD（Demo版）

## 1. 项目概述

### 1.1 项目名称

工业垂域模型端边云协同部署与运行系统（Demo版）

---

### 1.2 项目目标

构建一个面向离散制造场景的工业AI模型端边云协同部署与运行平台Demo，展示工业垂域模型在制造现场的：

* 智能部署
* 推理调度
* 推理链编排
* 运行监测
* 资源优化

能力。

本系统当前阶段：

> 仅实现前端可视化Demo，不涉及真实后端算法能力。

---

### 1.3 Demo定位

本Demo主要用于：

* 项目汇报
* 专家评审
* 技术路线展示
* 产品方案演示
* UI原型验证

重点体现：

> “工业AI模型如何在复杂制造现场稳定运行”

---

# 2. 用户角色

| 角色     | 权限      |
| ------ | ------- |
| 管理员    | 全部功能    |
| 模型开发人员 | 模型上传、部署 |
| 运维人员   | 运行监测、调优 |
| 企业用户   | 查看运行状态  |

Demo阶段不做权限控制，仅保留UI。

---

# 3. 系统整体结构

系统采用左侧菜单布局。

一级菜单如下：

```text
1. 驾驶舱
2. 模型部署中心
3. 推理引擎中心
4. 推理链编排中心
5. 运行监测中心
6. 资源调度中心
7. 模型仓库
8. 节点管理
```

---

# 4. UI整体风格

## 4.1 风格

工业科技风：

* 深色背景
* 蓝色高亮
* 数据流动画
* 卡片式布局
* 实时状态动态刷新

参考风格：

* Kubernetes Dashboard
* Grafana
* 工业互联网平台
* AI算力调度平台

---

## 4.2 技术建议

| 技术             | 用途       |
| -------------- | -------- |
| React          | 前端框架     |
| Ant Design Pro | 后台框架     |
| ECharts        | 图表       |
| AntV X6        | 拓扑图/流程编排 |
| Mock.js        | 假数据      |
| Framer Motion  | 动画       |

---

# 5. 页面详细设计

---

# 5.1 驾驶舱

## 页面目标

展示端边云整体运行状态。

---

## 页面布局

### 顶部区域

显示：

| 内容    | 示例      |
| ----- | ------- |
| 当前场景  | 白车身焊接产线 |
| 在线模型数 | 26      |
| 在线节点数 | 18      |
| 当前告警数 | 2       |

---

### 左侧区域：制造场景树

树形结构：

```text
焊接车间
 ├─ 焊点检测
 ├─ 焊缝质量检测
打磨车间
装配车间
```

点击后切换场景。

---

### 中央区域：端边云拓扑图（核心）

展示：

* 云中心
* 边缘节点
* 工业终端
* PLC
* 工业相机
* 机器人

节点间显示：

* 模型下发流
* 推理数据流
* 状态监测流

要求：

* 数据流动动画
* 节点状态颜色变化
* 支持缩放拖拽

---

### 右侧区域：实时性能指标

图表：

| 指标    | 类型  |
| ----- | --- |
| 推理延迟  | 折线图 |
| GPU占用 | 仪表盘 |
| 带宽占用  | 面积图 |
| 推理吞吐量 | 柱状图 |

---

### 底部区域：推理链状态

显示：

```text
视觉检测模型
 ↓
缺陷识别模型
 ↓
工艺决策模型
 ↓
机器人执行模型
```

状态：

* Running
* Warning
* Offline

---

# 5.2 模型部署中心

## 页面目标

展示模型自动部署能力。

---

## 页面布局

### 左侧：模型列表

卡片展示：

| 字段   | 示例            |
| ---- | ------------- |
| 模型名称 | WeldDetect-v2 |
| 模型类型 | 视觉检测          |
| 模型大小 | 2.3GB         |
| 当前状态 | 未部署           |

支持：

* 搜索
* 筛选
* 上传按钮

---

### 中间：部署策略推荐

展示：

```text
推荐部署方案：

端侧：
轻量预检测

边缘：
实时推理

云端：
模型训练与优化
```

展示维度：

* 时延
* 成本
* 能耗
* 精度
* 隐私

---

### 右侧：部署拓扑图

支持：

* 节点拖拽
* 模型拖拽部署
* 动态部署动画

部署过程动画：

```text
模型封装
 ↓
环境编译
 ↓
镜像生成
 ↓
模型下发
 ↓
运行启动
```

---

### 底部：部署日志

实时滚动：

```text
[INFO] 模型封装完成
[INFO] Edge-03部署成功
[WARN] GPU资源不足
```

---

# 5.3 推理引擎中心

## 页面目标

展示异构推理引擎能力。

---

## 页面布局

### 左侧：模型运行列表

显示：

* 当前运行模型
* 推理框架
* 当前设备

---

### 中间：异构推理架构图

展示：

```text
CPU
GPU
NPU
FPGA
```

算子在不同硬件间流转。

要求：

* 动态动画
* 数据流转效果

---

### 右侧：实时推理指标

| 指标     | 示例   |
| ------ | ---- |
| 当前延迟   | 68ms |
| 推理QPS  | 240  |
| GPU利用率 | 82%  |
| 算子命中率  | 91%  |

---

### 底部：优化效果对比

展示：

```text
优化前：120ms
优化后：68ms
```

使用趋势图展示。

---

# 5.4 推理链编排中心

## 页面目标

展示工业AI推理链自动编排能力。

---

## 页面布局

采用：

> 可视化拖拽编排

类似：

* Dify
* LangFlow
* Node-RED

---

## 节点类型

| 节点    | 类型   |
| ----- | ---- |
| 视觉模型  | AI节点 |
| OCR模型 | AI节点 |
| 工艺决策  | 逻辑节点 |
| PLC控制 | 执行节点 |
| 报警模块  | 输出节点 |

---

## 功能

支持：

* 节点拖拽
* 节点连线
* 参数配置
* 推理链保存
* 推理链运行

---

## 运行效果

链路运行时：

* 节点高亮
* 流光动画
* 实时输出结果

---

# 5.5 运行监测中心

## 页面目标

展示系统实时运行状态。

---

## 页面布局

### 左侧：节点监测

显示：

| 节点       | 状态 |
| -------- | -- |
| Edge-01  | 在线 |
| Edge-02  | 告警 |
| Cloud-01 | 正常 |

---

### 中间：实时告警

显示：

```text
GPU利用率过高
推理延迟异常
网络带宽波动
```

支持：

* 告警等级颜色
* 告警时间轴

---

### 右侧：趋势监测图

图表：

* CPU
* GPU
* 内存
* 带宽
* 推理延迟

---

### 底部：系统日志

滚动日志窗口。

---

# 5.6 资源调度中心

## 页面目标

展示系统自动优化能力。

---

## 页面布局

### 顶部：资源概览

显示：

* CPU
* GPU
* NPU
* 存储
* 网络

---

### 中间：资源热力图

展示：

* 节点负载
* 网络负载
* GPU占用

---

### 右侧：调度策略

显示：

```text
当前策略：
负载均衡优先
```

支持切换：

* 时延优先
* 能耗优先
* 成本优先

---

### 底部：动态调度事件

示例：

```text
Edge-02负载过高
→ 自动迁移推理任务至Cloud-01
```

要求：

* 动态动画
* 调度路径变化

---

# 5.7 模型仓库

## 页面目标

统一管理工业AI模型。

---

## 页面布局

表格形式：

| 字段   | 示例            |
| ---- | ------------- |
| 模型名称 | WeldDetect-v2 |
| 模型类型 | 焊接检测          |
| 推理框架 | TensorRT      |
| 版本   | v2.1          |
| 部署节点 | Edge-03       |
| 状态   | Running       |

支持：

* 搜索
* 筛选
* 查看详情
* 删除
* 发布

---

# 5.8 节点管理

## 页面目标

展示端边云节点管理能力。

---

## 页面布局

树状结构：

```text
云中心
 ├─ Cloud-01
边缘节点
 ├─ Edge-01
 ├─ Edge-02
工业终端
 ├─ Camera-01
 ├─ PLC-01
```

---

## 节点详情

显示：

| 字段   | 示例   |
| ---- | ---- |
| CPU  | 48%  |
| GPU  | 72%  |
| 内存   | 64GB |
| 网络状态 | 正常   |

---

# 6. Demo核心演示流程（必须实现）

---

## 场景：焊接缺陷检测

---

## Step1 上传模型

用户上传：

```text
焊接缺陷检测模型
```

---

## Step2 自动生成部署策略

系统展示：

```text
端：
轻量检测

边：
实时推理

云：
持续训练
```

---

## Step3 一键部署

播放：

* 封装
* 编译
* 下发
* 启动

动画。

---

## Step4 推理运行

工业相机画面流动。

实时输出：

```text
焊缝异常
质量评分
缺陷报警
```

---

## Step5 运行异常

系统提示：

```text
Edge-02 GPU负载过高
```

---

## Step6 自动优化

系统自动：

```text
迁移部分推理至云端
```

---

## Step7 恢复稳定

展示：

```text
推理延迟：
112ms → 63ms
```

---

# 7. 假数据要求

所有数据均使用Mock。

需要模拟：

| 数据     | 内容    |
| ------ | ----- |
| 节点状态   | 在线/离线 |
| GPU占用  | 动态变化  |
| 推理延迟   | 波动    |
| 模型运行日志 | 实时刷新  |
| 告警事件   | 定时触发  |

---

# 8. 动效要求

必须有：

| 动效    | 说明   |
| ----- | ---- |
| 数据流动画 | 拓扑流动 |
| 节点闪烁  | 告警   |
| 数字滚动  | 实时指标 |
| 流光连线  | 推理链  |
| 状态切换  | 动态部署 |

---

# 9. Demo阶段不实现内容

以下内容仅展示UI：

* 真模型推理
* 真容器部署
* 真K8S调度
* 真GPU调度
* 真AI算法
* 真资源监测

全部采用：

```text
Mock数据 + 动态动画
```

实现。
