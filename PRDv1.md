# 《工业模型适配与协同运行系统》PRD（Demo版 v2.0）

> 基于 v1.0 优化版本，采纳 P0 + P1 优化建议
> 核心定位：解决工业模型不易用、落地频繁适配的问题

---

## 1. 项目概述

### 1.1 项目名称

工业模型适配与协同运行系统（Demo版）

> 全称：工业垂域模型适配与端边云协同部署运行系统（简称：工业模型适配与协同运行系统）

---

### 1.2 项目背景与核心问题

#### 行业痛点

工业AI模型在落地过程中面临的核心问题不是"部署"本身，而是**模型不易用、落地频繁适配**：

| 问题维度 | 具体表现 | 影响 |
|----------|----------|------|
| **硬件兼容** | 不同工厂的相机、PLC、机器人品牌型号各异 | 每次落地需重新适配硬件接口 |
| **协议适配** | Modbus、Profinet、OPC UA 等工业协议不统一 | 协议转换工作量大 |
| **环境差异** | 光照、噪声、震动、温湿度等环境条件不同 | 模型精度下降，需反复调参 |

**现状：** 每次将模型落地到一个新工厂/新产线，都需要大量手工适配工作，成本高、周期长、难以规模化。

#### 本系统的核心使命

> **让工业模型适配工作自动化、可复用、可对比，大幅降低模型落地成本。**

---

### 1.3 项目目标

构建一个面向离散制造场景的工业AI模型**适配**与协同运行平台Demo，展示：

- **智能适配**：模型与目标环境的自动兼容性评估与适配
- **适配复用**：适配方案模板化，一次适配多处复用
- **智能部署**：适配后的模型自动部署到端边云
- **推理调度**：异构硬件上的推理优化
- **推理链编排**：工业AI推理链可视化编排
- **运行监测**：适配后的系统运行状态监测
- **资源优化**：基于负载的动态资源调度

---

### 1.4 Demo定位

本Demo主要用于：

- 项目汇报
- 专家评审
- 技术路线展示
- 产品方案演示
- UI原型验证

重点体现：

> "工业AI模型如何在复杂制造现场快速适配并稳定运行"

---

## 2. 用户角色

| 角色 | 权限 | 核心关注 |
|------|------|----------|
| 管理员 | 全部功能 | 系统整体运行 |
| 模型开发人员 | 模型上传、适配配置、部署 | 模型能否快速适配落地 |
| 运维人员 | 运行监测、调优、适配维护 | 适配后的系统稳定性 |
| 企业用户 | 查看运行状态、适配进度 | 适配成本和周期 |

Demo阶段不做权限控制，仅保留UI。

---

## 3. 系统整体结构

系统采用左侧菜单布局。

一级菜单如下：

```
1. 驾驶舱
2. 模型适配中心      ← 核心页面
3. 模型部署中心
4. 推理引擎中心
5. 推理链编排中心
6. 运行监测中心
7. 资源调度中心
8. 模型仓库
9. 节点管理
```

---

## 4. UI整体风格

### 4.1 风格

工业科技风：

- 深色背景
- 蓝色高亮
- 数据流动画
- 卡片式布局
- 实时状态动态刷新

参考风格：

- Kubernetes Dashboard
- Grafana
- 工业互联网平台
- AI算力调度平台

---

### 4.2 技术选型

| 技术 | 用途 | 说明 |
|------|------|------|
| React 18 | 前端框架 | |
| TypeScript | 开发语言 | 全项目使用，提高代码可维护性 |
| Vite | 构建工具 | 快速开发体验，HMR 热更新 |
| Ant Design Pro | 后台框架 | |
| Zustand | 状态管理 | 轻量简洁，适合 Demo 场景 |
| React Router v6 | 路由方案 | |
| ECharts | 图表 | |
| AntV X6 | 拓扑图/流程编排 | |
| MSW (Mock Service Worker) | Mock 数据 | 替代 Mock.js，拦截请求更接近真实 API |
| Framer Motion | 动画 | |

---

### 4.3 项目目录结构

```
indus-ai-demo/
├── public/
├── src/
│   ├── api/                 # MSW handlers + API 类型定义
│   ├── components/          # 公共组件
│   │   ├── LoadingState/    # 加载态组件
│   │   ├── EmptyState/      # 空态组件
│   │   ├── ErrorState/      # 错误态组件
│   │   └── SceneSwitcher/   # 场景切换器
│   ├── layouts/             # 布局组件
│   ├── pages/               # 9个页面
│   │   ├── Dashboard/       # 驾驶舱
│   │   ├── ModelAdapt/      # 模型适配中心
│   │   ├── ModelDeploy/     # 模型部署中心
│   │   ├── InferenceEngine/ # 推理引擎中心
│   │   ├── PipelineEditor/  # 推理链编排中心
│   │   ├── Monitor/         # 运行监测中心
│   │   ├── ResourceScheduler/ # 资源调度中心
│   │   ├── ModelRepo/       # 模型仓库
│   │   └── NodeManager/     # 节点管理
│   ├── stores/              # Zustand stores
│   ├── mocks/               # MSW mock 数据
│   │   ├── handlers/        # API handlers
│   │   ├── data/            # Mock 数据
│   │   └── scenarios/       # 场景配置（正常/高负载/故障）
│   ├── types/               # TypeScript 类型定义
│   ├── utils/               # 工具函数
│   ├── App.tsx
│   └── main.tsx
├── package.json
└── tsconfig.json
```

---

## 5. 页面详细设计

---

### 5.1 驾驶舱

#### 页面目标

展示端边云整体运行状态，突出适配后的系统健康度。

#### 页面布局

##### 顶部区域

显示：

| 内容 | 示例 |
|------|------|
| 当前场景 | 白车身焊接产线 |
| 在线模型数 | 26 |
| 在线节点数 | 18 |
| 当前告警数 | 2 |
| 适配完成数 | 12 |
| **适配健康度** | **92%** ← 新增 |

##### 左侧区域：制造场景树

树形结构：

```
焊接车间
 ├─ 焊点检测
 ├─ 焊缝质量检测
打磨车间
装配车间
```

点击后切换场景。

##### 中央区域：端边云拓扑图（核心）

展示：

- 云中心
- 边缘节点
- 工业终端
- PLC
- 工业相机
- 机器人

节点间显示：

- 模型下发流
- 推理数据流
- 状态监测流
- 适配数据流

要求：

- 数据流动画
- 节点状态颜色变化
- 支持缩放拖拽
- 节点点击弹窗显示详情
- 右键菜单
- 图例说明

##### 右侧区域：实时性能指标

图表：

| 指标 | 类型 |
|------|------|
| 推理延迟 | 折线图 |
| GPU占用 | 仪表盘 |
| 带宽占用 | 面积图 |
| 推理吞吐量 | 柱状图 |
| **适配健康度** | **环形图** ← 新增 |

##### 底部区域：推理链状态

显示：

```
视觉检测模型
 ↓
缺陷识别模型
 ↓
工艺决策模型
 ↓
机器人执行模型
```

状态：

- Running
- Warning
- Offline

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 骨架屏 + 图表 loading 动画 |
| 空态 | 首次进入时显示引导提示 |
| 错误态 | 网络错误时显示重试按钮 |
| 边界态 | 大量节点时拓扑图自动缩放适配 |

---

### 5.2 模型适配中心（核心页面）

#### 页面目标

**这是本系统的核心页面。** 展示工业模型如何快速适配不同工厂环境，解决"模型不易用"的问题。

展示三大核心能力：

1. **适配自动化**：自动评估模型与目标环境的兼容性，推荐适配方案
2. **适配复用**：适配方案模板化，一次适配可复用到相似场景
3. **适配对比**：适配前后的效果对比，量化适配收益

---

#### 页面布局

##### 顶部：适配概览

| 指标 | 示例 | 说明 |
|------|------|------|
| 待适配模型 | 5 | 等待适配的模型数量 |
| 已完成适配 | 12 | 已成功适配的模型数量 |
| 适配成功率 | 92% | 历史适配成功率 |
| 平均适配周期 | 2.3天 | 从开始适配到完成的天数 |
| 适配模板数 | 8 | 可复用的适配方案模板数量 |

---

##### 左侧：适配任务列表

展示当前所有适配任务：

| 字段 | 示例 |
|------|------|
| 模型名称 | WeldDetect-v2 |
| 目标工厂 | 焊接车间-A线 |
| 适配状态 | 进行中 / 已完成 / 失败 |
| 兼容性评分 | 85% |
| 适配进度 | 60% |
| 负责人 | 张三 |

支持：

- 搜索/筛选（按状态、工厂、模型类型）
- 新建适配任务按钮
- 适配任务详情查看

---

##### 中间-上：适配工作流（核心交互区）

展示适配的完整流程，当前步骤高亮，支持**自动适配**和**手动引导适配**两种模式切换：

```
┌─ 适配模式 ─────────────────────────────┐
│  ○ 自动适配（推荐）  ○ 手动引导适配       │
└────────────────────────────────────────┘
```

**自动适配模式：** 系统自动执行所有步骤，用户只需监控进度。
**手动引导适配模式：** 每步完成后需用户确认，支持参数调整和回退。

```
Step 1: 环境检测
  ├─ 检测目标产线硬件配置（相机型号、PLC型号、机器人型号）
  ├─ 检测通信协议（Modbus/Profinet/OPC UA）
  └─ 检测环境条件（光照、噪声、温湿度）
  ↓

Step 2: 兼容性评估
  ├─ 模型与硬件的兼容性评分
  ├─ 协议适配难度评估
  ├─ 环境差异对精度的影响预测
  └─ 总体适配建议（推荐/不推荐/需调整）
  ↓

Step 3: 适配配置
  ├─ 硬件适配器选择（自动推荐最佳适配器）
  ├─ 协议参数配置（自动填充默认值）
  ├─ 环境补偿参数配置（图像增强、滤波等）
  └─ 适配方案预览
  ↓

Step 4: 适配执行
  ├─ 适配进度条
  ├─ 实时适配日志
  └─ 各步骤状态（成功/警告/失败）
  ↓

Step 5: 适配验证
  ├─ 适配前 vs 适配后效果对比
  ├─ 精度对比（原始精度 → 适配后精度）
  ├─ 延迟对比
  ├─ 验证结论
  └─ [导出适配报告] ← 新增
```

**回退机制：** 手动模式下，每一步都支持"回退到上一步"和"保存当前进度"。

---

##### 中间-下：适配对比视图

**适配前后效果对比（核心展示）：**

| 对比项 | 适配前 | 适配后 | 优化幅度 |
|--------|--------|--------|----------|
| 模型精度 | 72% | 94% | ↑ 30.6% |
| 推理延迟 | 180ms | 65ms | ↓ 63.9% |
| 硬件兼容性 | 不兼容 | 完全兼容 | ✅ |
| 协议适配 | 不支持 | 自动适配 | ✅ |

使用双柱状图或对比折线图展示。

---

##### 右侧：适配方案推荐

**自动推荐的适配方案：**

```
┌─────────────────────────────────┐
│  推荐适配方案 v3.2              │
│                                 │
│  硬件适配：                      │
│  ├─ 相机适配器: HikVision-CAM-v2 │
│  ├─ PLC适配器:  Siemens-S7-v1    │
│  └─ 机器人适配器: ABB-IRB-v3     │
│                                 │
│  协议适配：                      │
│  ├─ 主协议: OPC UA               │
│  └─ 备选协议: Modbus TCP         │
│                                 │
│  环境补偿：                      │
│  ├─ 图像增强: 低光照模式          │
│  ├─ 降噪滤波: 中值滤波 3x3       │
│  └─ 精度校准: 自动校准            │
│                                 │
│  预估适配周期: 2天               │
│  预估成功率: 92%                 │
│                                 │
│  [一键应用适配方案]               │
└─────────────────────────────────┘
```

支持：

- 方案详情展开
- 方案对比（多个候选方案）
- 手动调整方案参数
- 保存为适配模板

---

##### 底部-左：适配模板库

预置适配模板：

| 模板名称 | 适用场景 | 硬件组合 | 协议 | 使用次数 | 成功率 |
|----------|----------|----------|------|----------|--------|
| 焊接线标准适配 | 白车身焊接 | 海康相机+西门子PLC+ABB机器人 | OPC UA | 15次 | 95% |
| 装配线标准适配 | 零部件装配 | 基恩士PLC+发那科机器人 | Profinet | 8次 | 90% |
| 打磨车间适配 | 表面打磨 | 海康相机+三菱PLC | Modbus TCP | 5次 | 88% |
| 质检通用适配 | 通用质检 | 多品牌兼容 | 多协议兼容 | 20次 | 92% |

支持：

- 一键应用模板
- 模板详情查看
- 基于模板创建新适配任务
- 模板评分和反馈

---

##### 底部-右：适配器市场（新增）

展示可用的硬件适配器列表：

| 适配器名称 | 类型 | 适用品牌 | 版本 | 使用次数 |
|------------|------|----------|------|----------|
| HikVision-CAM-v2 | 相机适配器 | 海康威视 | v2.1 | 25次 |
| Siemens-S7-v1 | PLC适配器 | 西门子 | v1.3 | 18次 |
| ABB-IRB-v3 | 机器人适配器 | ABB | v3.0 | 12次 |
| Modbus-TCP-v2 | 协议适配器 | 通用 | v2.0 | 30次 |
| OPC-UA-v1 | 协议适配器 | 通用 | v1.5 | 22次 |
| Fanuc-Robot-v2 | 机器人适配器 | 发那科 | v2.1 | 8次 |

支持：

- 搜索/筛选（按类型、品牌）
- 适配器详情查看（支持的型号、版本历史）
- 适配器评分和反馈

---

##### 底部-扩展：适配历史时间线（新增）

展示历史适配记录的时间线视图：

```
2026-05-08  WeldDetect-v2 → 焊接车间-A线  ✅ 成功  耗时 2.1天
2026-05-06  AssemblyCheck-v1 → 装配线-B线  ✅ 成功  耗时 1.8天
2026-05-03  SurfaceDefect-v3 → 打磨车间-C线  ❌ 失败  原因：硬件不兼容
2026-04-28  QualityCheck-v2 → 质检线-D线  ✅ 成功  耗时 3.2天
```

支持：

- 按时间、模型、工厂筛选
- 查看每次适配的详细配置和日志
- 失败记录高亮显示，支持重新适配

---

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 适配流程骨架屏 |
| 空态 | 无适配任务时显示"创建第一个适配任务"引导 |
| 错误态 | 适配失败时步骤标红 + 错误详情 + 重试/回退选项 |
| 边界态 | 大量适配任务时分页 + 批量操作 |

---

### 5.3 模型部署中心

#### 页面目标

展示模型适配后的自动部署能力。

#### 页面布局

##### 左侧：模型列表

卡片展示：

| 字段 | 示例 |
|------|------|
| 模型名称 | WeldDetect-v2 |
| 模型类型 | 视觉检测 |
| 模型大小 | 2.3GB |
| 适配状态 | 已适配 |
| 当前状态 | 未部署 |

支持：

- 搜索
- 筛选（含按适配状态筛选）
- 上传按钮

##### 中间：部署策略推荐

展示：

```
推荐部署方案：

端侧：
轻量预检测

边缘：
实时推理

云端：
模型训练与优化
```

展示维度：

- 时延
- 成本
- 能耗
- 精度
- 隐私

##### 右侧：部署拓扑图

支持：

- 节点拖拽
- 模型拖拽部署
- 动态部署动画

部署过程动画：

```
模型封装
 ↓
环境编译
 ↓
镜像生成
 ↓
模型下发
 ↓
运行启动
```

##### 底部：部署日志

实时滚动：

```
[INFO] 模型封装完成
[INFO] 适配配置已加载
[INFO] Edge-03部署成功
[WARN] GPU资源不足
```

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 卡片骨架屏 |
| 空态 | 无模型时显示"上传第一个模型"引导 |
| 错误态 | 部署失败时日志区域红色高亮 + 重试按钮 |
| 边界态 | 模型列表超过 20 个时分页 |

---

### 5.4 推理引擎中心

#### 页面目标

展示异构推理引擎能力。

#### 页面布局

##### 左侧：模型运行列表

显示：

- 当前运行模型
- 推理框架
- 当前设备

##### 中间：异构推理架构图

展示：

```
CPU
GPU
NPU
FPGA
```

算子在不同硬件间流转。

要求：

- 动态动画
- 数据流转效果

##### 右侧：实时推理指标

| 指标 | 示例 |
|------|------|
| 当前延迟 | 68ms |
| 推理QPS | 240 |
| GPU利用率 | 82% |
| 算子命中率 | 91% |

##### 底部：优化效果对比

展示：

```
优化前：120ms
优化后：68ms
```

使用趋势图展示。

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 指标卡片骨架屏 |
| 空态 | 无运行模型时显示"部署模型后查看"提示 |
| 错误态 | 推理异常时指标标红 |
| 边界态 | 多模型运行时列表可滚动 |

---

### 5.5 推理链编排中心

#### 页面目标

展示工业AI推理链可视化编排能力。

#### 页面布局

采用：

> 可视化拖拽编排

类似：

- Dify
- LangFlow
- Node-RED

#### 节点类型

| 节点 | 类型 |
|------|------|
| 视觉模型 | AI节点 |
| OCR模型 | AI节点 |
| 工艺决策 | 逻辑节点 |
| PLC控制 | 执行节点 |
| 报警模块 | 输出节点 |

#### 功能

支持：

- 节点拖拽
- 节点连线
- 参数配置
- 推理链保存
- 推理链运行

#### 推理链模板库

预置模板：

| 模板名称 | 包含节点 | 说明 |
|----------|----------|------|
| 焊接缺陷检测链 | 视觉模型 → 缺陷识别 → 报警模块 | 焊接场景标准流程 |
| 装配质量检测链 | 视觉模型 → OCR模型 → 工艺决策 → PLC控制 | 装配场景标准流程 |
| 质量综合检测链 | 视觉模型 → 工艺决策 → 报警模块 → PLC控制 | 综合质检流程 |

支持：

- 一键加载模板到画布
- 模板预览
- 基于模板二次编辑

#### 运行效果

链路运行时：

- 节点高亮
- 流光动画
- 实时输出结果

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 画布骨架屏 |
| 空态 | 空白画布 + "从模板创建或拖拽节点开始"引导 |
| 错误态 | 推理链运行失败时节点标红 + 错误提示 |
| 边界态 | 复杂链路支持缩略图导航 |

---

### 5.6 运行监测中心

#### 页面目标

展示系统实时运行状态，包括适配后的运行健康度。

#### 页面布局

##### 左侧：节点监测

显示：

| 节点 | 状态 | 适配状态 |
|------|------|----------|
| Edge-01 | 在线 | 已适配 |
| Edge-02 | 告警 | 适配中 |
| Cloud-01 | 正常 | 已适配 |

##### 中间：实时告警

显示：

```
GPU利用率过高
推理延迟异常
网络带宽波动
适配兼容性警告
```

支持：

- 告警等级颜色
- 告警时间轴

##### 右侧：趋势监测图

图表：

- CPU
- GPU
- 内存
- 带宽
- 推理延迟
- 适配质量评分

##### 底部：系统日志

滚动日志窗口。

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 图表骨架屏 |
| 空态 | 无告警时显示"系统运行正常"状态 |
| 错误态 | 监测数据中断时显示断连提示 |
| 边界态 | 大量告警时支持分组折叠 |

---

### 5.7 资源调度中心

#### 页面目标

展示系统自动优化能力。

#### 页面布局

##### 顶部：资源概览

显示：

- CPU
- GPU
- NPU
- 存储
- 网络

##### 中间：资源热力图

展示：

- 节点负载
- 网络负载
- GPU占用

##### 右侧：调度策略

显示：

```
当前策略：
负载均衡优先
```

支持切换：

- 时延优先
- 能耗优先
- 成本优先

##### 底部：动态调度事件

示例：

```
Edge-02负载过高
→ 自动迁移推理任务至Cloud-01
```

要求：

- 动态动画
- 调度路径变化

##### 调度前后对比视图

展示调度优化前后的指标对比：

| 指标 | 调度前 | 调度后 | 优化幅度 |
|------|--------|--------|----------|
| 推理延迟 | 112ms | 63ms | ↓ 43.8% |
| GPU负载率 | 92% | 67% | ↓ 27.2% |
| 能耗 | 850W | 620W | ↓ 27.1% |
| 吞吐量 | 180 QPS | 240 QPS | ↑ 33.3% |

使用双柱状图或对比折线图展示。

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 热力图骨架屏 |
| 空态 | 无调度事件时显示"系统运行平稳" |
| 错误态 | 调度失败时事件列表标红 |
| 边界态 | 大量节点时热力图支持缩放 |

---

### 5.8 模型仓库

#### 页面目标

统一管理工业AI模型，包括适配信息。

#### 页面布局

表格形式：

| 字段 | 示例 |
|------|------|
| 模型名称 | WeldDetect-v2 |
| 模型类型 | 焊接检测 |
| 推理框架 | TensorRT |
| 版本 | v2.1 |
| 适配状态 | 已适配 |
| 适配目标 | 焊接车间-A线 |
| 部署节点 | Edge-03 |
| 状态 | Running |

支持：

- 搜索
- 筛选（含按适配状态筛选）
- 查看详情（含适配详情）
- 删除
- 发布

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 表格骨架屏 |
| 空态 | 无模型时显示"上传第一个模型"引导 |
| 错误态 | 加载失败时显示重试按钮 |
| 边界态 | 大量模型时分页 + 批量选择 |

---

### 5.9 节点管理

#### 页面目标

展示端边云节点管理能力，包括节点的适配兼容性信息。

#### 页面布局

树状结构：

```
云中心
 ├─ Cloud-01
边缘节点
 ├─ Edge-01
 ├─ Edge-02
工业终端
 ├─ Camera-01 (海康威视 DS-2CD)
 ├─ PLC-01 (西门子 S7-1200)
```

#### 节点详情

显示：

| 字段 | 示例 |
|------|------|
| CPU | 48% |
| GPU | 72% |
| 内存 | 64GB |
| 网络状态 | 正常 |
| 兼容模型数 | 5 |
| 已适配协议 | OPC UA, Modbus |

#### 兼容性矩阵（新增）

展示"节点 × 模型"的兼容性热力图：

```
           WeldDetect  AssemblyCheck  SurfaceDefect
Edge-01       ✅            ✅            ⚠️
Edge-02       ✅            ❌            ✅
Cloud-01      ✅            ✅            ✅
Camera-01     ✅            ⚠️            ❌
PLC-01        ✅            ✅            ⚠️
```

- ✅ 完全兼容
- ⚠️ 部分兼容（需适配）
- ❌ 不兼容

##### 页面状态设计

| 状态 | 表现 |
|------|------|
| 加载态 | 树结构骨架屏 |
| 空态 | 无节点时显示"添加第一个节点"引导 |
| 错误态 | 节点离线时标红 + 重连按钮 |
| 边界态 | 大量节点时树结构支持搜索过滤 |

---

## 6. Demo核心演示流程（必须实现）

---

### 场景：焊接缺陷检测 — 从适配到部署运行

> 相比 v1.0，本演示流程的核心变化是：**将"适配"作为第一步，贯穿整个演示。**

---

#### Step1 上传模型

用户上传：

```
焊接缺陷检测模型 WeldDetect-v2
```

---

#### Step2 自动适配（核心步骤）

系统自动执行适配流程，支持**自动**和**手动引导**两种模式：

**自动模式（默认）：**

```
Step 2.1 环境检测
  → 检测到目标产线：焊接车间-A线
  → 检测到硬件：海康相机 + 西门子PLC + ABB机器人
  → 检测到协议：OPC UA
  → 环境条件：低光照、高噪声

Step 2.2 兼容性评估
  → 硬件兼容性：92%
  → 协议兼容性：100%
  → 环境适配难度：中等
  → 总体评估：推荐适配

Step 2.3 适配方案推荐
  → 推荐使用"焊接线标准适配"模板
  → 自动选择适配器：HikVision-CAM-v2 + Siemens-S7-v1 + ABB-IRB-v3
  → 环境补偿：低光照增强 + 中值滤波

Step 2.4 一键适配
  → [INFO] 硬件适配器加载完成
  → [INFO] 协议参数配置完成
  → [INFO] 环境补偿参数配置完成
  → [INFO] 适配完成！耗时：45秒

Step 2.5 适配验证
  → 适配前精度：72% → 适配后精度：94%
  → 适配前延迟：180ms → 适配后延迟：65ms
  → 结论：适配成功 ✓
  → [导出适配报告]
```

**手动引导模式：** 每步完成后弹出确认对话框，用户可调整参数或回退到上一步。

---

#### Step3 自动生成部署策略

系统展示：

```
端：
轻量检测（适配后模型）

边：
实时推理（适配后模型）

云：
持续训练（原始模型）
```

---

#### Step4 一键部署

播放：

- 封装
- 编译
- 下发
- 启动

动画。

---

#### Step5 推理运行

工业相机画面流动。

实时输出：

```
焊缝异常
质量评分
缺陷报警
```

---

#### Step6 运行异常

系统提示：

```
Edge-02 GPU负载过高
```

---

#### Step7 自动优化

系统自动：

```
迁移部分推理至云端
```

---

#### Step8 恢复稳定

展示：

```
推理延迟：
112ms → 63ms
```

---

### 模拟场景切换

在全局顶部栏增加场景切换器，支持三种模式：

| 模式 | 效果 |
|------|------|
| 🟢 正常模式 | 系统平稳运行，指标正常波动 |
| 🟡 高负载模式 | GPU 利用率飙升到 90%+，延迟增加，触发告警 |
| 🔴 故障模式 | 节点离线、推理异常、告警频繁触发 |

切换后全局 Mock 数据自动变化，展示系统在不同状态下的表现。

---

## 7. 假数据要求

所有数据均使用 MSW (Mock Service Worker) 模拟。

需要模拟：

| 数据 | 内容 |
|------|------|
| 节点状态 | 在线/离线 |
| GPU占用 | 动态变化 |
| 推理延迟 | 波动 |
| 模型运行日志 | 实时刷新 |
| 告警事件 | 定时触发 |
| 适配任务 | 适配进度、兼容性评分 |
| 适配模板 | 模板列表、使用次数 |
| 适配对比数据 | 适配前后精度、延迟对比 |
| **适配器列表** | **适配器名称、类型、版本** ← 新增 |
| **适配历史** | **历史适配记录时间线** ← 新增 |
| **兼容性矩阵** | **节点×模型兼容性数据** ← 新增 |

### API 接口设计

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/dashboard/overview | GET | 驾驶舱概览数据 |
| /api/dashboard/topology | GET | 拓扑图数据 |
| /api/dashboard/metrics | GET | 实时性能指标 |
| /api/adapt/tasks | GET/POST | 适配任务列表/创建 |
| /api/adapt/tasks/:id | GET | 适配任务详情 |
| /api/adapt/tasks/:id/execute | POST | 执行适配 |
| /api/adapt/tasks/:id/rollback | POST | 回退适配步骤 ← 新增 |
| /api/adapt/tasks/:id/report | GET | 导出适配报告 ← 新增 |
| /api/adapt/templates | GET | 适配模板列表 |
| /api/adapt/comparison | GET | 适配前后对比数据 |
| **/api/adapt/adapters** | **GET** | **适配器市场列表** ← 新增 |
| **/api/adapt/history** | **GET** | **适配历史记录** ← 新增 |
| /api/models | GET/POST | 模型列表/上传 |
| /api/models/:id/deploy | POST | 模型部署 |
| /api/inference/engines | GET | 推理引擎列表 |
| /api/inference/metrics | GET | 推理指标 |
| /api/pipelines | GET/POST | 推理链列表/创建 |
| /api/pipelines/templates | GET | 推理链模板列表 |
| /api/monitor/nodes | GET | 节点监测数据 |
| /api/monitor/alerts | GET | 告警列表 |
| /api/monitor/logs | GET | 系统日志 |
| /api/resources/overview | GET | 资源概览 |
| /api/resources/heatmap | GET | 资源热力图 |
| /api/resources/schedule | POST | 调度策略切换 |
| /api/resources/comparison | GET | 调度前后对比数据 |
| /api/nodes | GET | 节点列表 |
| **/api/nodes/compatibility** | **GET** | **节点兼容性矩阵** ← 新增 |
| /api/scenario | GET/PUT | 获取/切换模拟场景 |

---

## 8. 动效要求

必须有：

| 动效 | 说明 |
|------|------|
| 数据流动画 | 拓扑流动 |
| 节点闪烁 | 告警 |
| 数字滚动 | 实时指标 |
| 流光连线 | 推理链 |
| 状态切换 | 动态部署 |
| 适配流程动画 | 适配步骤流转 |
| 对比切换动画 | 适配前后效果切换 |

---

## 9. Demo阶段不实现内容

以下内容仅展示UI：

- 真模型推理
- 真容器部署
- 真K8S调度
- 真GPU调度
- 真AI算法
- 真资源监测
- 真硬件适配驱动

全部采用：

```
Mock数据 + 动态动画
```

实现。

---

## 10. 非功能需求

| 需求 | 说明 |
|------|------|
| 页面加载性能 | 首屏加载 < 3s |
| 浏览器兼容 | 支持 Chrome / Edge 最新版本 |
| 分辨率适配 | 适配 1920x1080 及以上分辨率 |
| 动画性能 | 拓扑图动画帧率 > 30fps |

---

## 附录：v1.0 → v2.0 变更记录

| 变更类型 | 变更内容 | 优先级 |
|----------|----------|--------|
| **重构** | **核心定位从"端边云部署"改为"模型适配+协同运行"** | **P0** |
| **新增** | **模型适配中心页面（完整设计）** | **P0** |
| **新增** | **适配工作流（5步 + 自动/手动双模式 + 回退机制）** | **P0** |
| **新增** | **适配模板库（4个预置模板）** | **P0** |
| **新增** | **适配前后对比视图** | **P0** |
| **新增** | **适配器市场（适配器列表）** | **P0** |
| **新增** | **适配历史时间线** | **P0** |
| **新增** | **适配报告导出** | **P0** |
| **新增** | **节点兼容性矩阵** | **P0** |
| **新增** | **驾驶舱适配健康度指标** | **P0** |
| **新增** | **演示流程增加 Step2 自动适配（双模式）** | **P0** |
| 新增 | 技术选型补充（TypeScript, Vite, Zustand, React Router v6, MSW） | P0 |
| 新增 | 项目目录结构 | P0 |
| 新增 | 每个页面的加载态/空态/错误态/边界态设计 | P0 |
| 新增 | MSW Mock 方案 + API 接口设计 | P0 |
| 新增 | 模拟场景切换（正常/高负载/故障） | P1 |
| 新增 | 推理链模板库（3个预置模板） | P1 |
| 新增 | 调度前后对比视图 | P1 |
| 新增 | 非功能需求 | P0 |
| 新增 | 拓扑图交互增强（点击弹窗、右键菜单、图